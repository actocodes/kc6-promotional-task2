[tool.uv]
workspace = { members = ["mcp_server", "agent_client"] }

[tool.uv.sources]
mcp_server = { workspace = true }
agent_client = { workspace = true }

[dependency-groups]
dev = [
    "mcp_server",
    "agent_client",
]